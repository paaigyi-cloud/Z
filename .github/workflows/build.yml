name: Build Flutter APK

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.19.0"
          channel: "stable"

      # ဘယ်လိုပုံစံနဲ့ပဲ ရေးထားရေးထား အတင်းအဓမ္မ Version 21 သို့ ပြောင်းခိုင်းမည့် အမိန့်ဖြစ်ပါသည်
      - run: |
          flutter create . --project-name=z_vpn --platforms=android
          git checkout lib/main.dart pubspec.yaml
          sed -i 's/minSdkVersion .*/minSdkVersion 21/g' android/app/build.gradle

      - run: flutter pub get
      - run: flutter build apk --release

      - uses: actions/upload-artifact@v4
        with:
          name: Z-VPN-Release-APK
          path: build/app/outputs/flutter-apk/app-release.apk
